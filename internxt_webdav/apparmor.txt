#include <tunables/global>

profile internxt_webdav flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
  network unix stream,

  signal (send, receive) set=(kill,term,int,hup,cont),

  /usr/local/bin/node rix,
  /usr/local/bin/internxt rix,
  /usr/local/bin/ha-entrypoint.mjs r,
  /usr/local/lib/internxt-ha/** r,

  /app/docker/entrypoint.sh rix,
  /app/bin/run.js rix,
  /app/dist/** rixm,
  /app/node_modules/** rixm,
  /app/** r,

  /bin/** rix,
  /usr/bin/** rix,
  /usr/lib/** rixm,
  /lib/** rixm,
  /lib64/** rixm,
  /etc/** r,
  /proc/** r,
  /sys/devices/system/cpu/** r,

  /dev/null rw,
  /dev/tty rw,
  /dev/random r,
  /dev/urandom r,

  /data/ rw,
  /data/** rwk,
  /tmp/** rwk,
  /run/** rwk,
}
